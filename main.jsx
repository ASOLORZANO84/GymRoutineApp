import React from "react";
import ReactDOM from "react-dom/client";
import GymRoutineTracker from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GymRoutineTracker />
  </React.StrictMode>
);